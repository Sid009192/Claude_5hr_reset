# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Playwright session profile (contains login cookies)
pw-profile/
conversation_url.txt

# Secrets — NEVER commit. Keep .env.example (template) tracked.
.env
.env.*
!.env.example

# Notification system: local config + runtime state
notify_config.local.toml
last_reset.txt
digest_staged.txt

# Google Calendar OAuth token cache (contains refresh token — NEVER commit)
token.json

# Markdown notes — ignore all except the GitHub README
*.md
!README.md
